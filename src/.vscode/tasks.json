{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/SparkFreelancer/SF.API/SF.API.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}